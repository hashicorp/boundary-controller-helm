# Copyright IBM Corp. 2026
# SPDX-License-Identifier: MPL-2.0

{{- if and .Values.bootstrapAdminAuthMethod.enabled (or .Release.IsInstall (and .Release.IsUpgrade .Values.bootstrapAdminAuthMethod.runOnUpgrade)) }}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "boundary.controller.serviceName" . }}-bootstrap-admin
  namespace: {{ include "boundary.namespace" . }}
  labels:
    {{- include "boundary.labels" . | nindent 4 }}
    app.kubernetes.io/component: controller-bootstrap-admin
  annotations:
    "helm.sh/hook": {{ if .Values.bootstrapAdminAuthMethod.runOnUpgrade }}post-install,post-upgrade{{ else }}post-install{{ end }}
    "helm.sh/hook-output-log-policy": hook-failed
    "helm.sh/hook-weight": "5"
    "helm.sh/hook-delete-policy": before-hook-creation
spec:
  backoffLimit: 2
  ttlSecondsAfterFinished: 600
  template:
    metadata:
      labels:
        {{- include "boundary.labels" . | nindent 8 }}
        app.kubernetes.io/component: controller-bootstrap-admin
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      automountServiceAccountToken: {{ .Values.serviceAccount.automountServiceAccountToken }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      serviceAccountName: {{ include "boundary.controller.serviceAccountName" . }}
      restartPolicy: Never
      containers:
        - name: bootstrap-admin
          image: "{{ include "boundary.controller.image" . }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          securityContext:
            {{- toYaml .Values.containerSecurityContext | nindent 12 }}
          env:
            - name: SKIP_SETCAP
              value: "1"
            - name: BOUNDARY_ADDR
              value: "{{ if not .Values.tls.disabled }}https{{ else }}http{{ end }}://{{ include "boundary.controller.apiServiceName" . }}:{{ .Values.controller.service.api.port }}"
            {{- if not .Values.tls.disabled }}
            - name: BOUNDARY_CACERT
              value: "{{ .Values.tls.mountPath }}/tls.crt"
            {{- end }}
            - name: BOUNDARY_ADMIN_USERNAME
              valueFrom:
                secretKeyRef:
                  name: {{ include "boundary.controller.secretName" . }}
                  key: {{ .Values.secretRefs.keys.adminUsername }}
            - name: BOUNDARY_ADMIN_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "boundary.controller.secretName" . }}
                  key: {{ .Values.secretRefs.keys.adminPassword }}
            - name: WAIT_TIMEOUT
              value: {{ .Values.bootstrapAdminAuthMethod.waitTimeoutSeconds | quote }}
            - name: BOUNDARY_AUTH_METHOD_NAME
              value: {{ .Values.bootstrapAdminAuthMethod.authMethodName | quote }}
            - name: BOUNDARY_USER_RESOURCE_NAME
              value: {{ .Values.bootstrapAdminAuthMethod.userResourceName | quote }}
            - name: BOUNDARY_ACCOUNT_RESOURCE_NAME
              value: {{ .Values.bootstrapAdminAuthMethod.accountResourceName | quote }}
            - name: BOUNDARY_ROLE_NAME
              value: {{ .Values.bootstrapAdminAuthMethod.roleName | quote }}
            {{- with .Values.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          command:
            - "sh"
            - "/etc/boundary/bootstrap-admin.sh"
          resources:
            {{- toYaml .Values.bootstrapAdminAuthMethod.resources | nindent 12 }}
          volumeMounts:
            - name: config
              mountPath: /etc/boundary
              readOnly: true
            {{- if not .Values.tls.disabled }}
            - name: tls-certs
              mountPath: {{ .Values.tls.mountPath }}
              readOnly: true
            {{- end }}
            - name: tmp
              mountPath: /tmp
            - name: boundary-data
              mountPath: /boundary
      volumes:
        - name: config
          configMap:
            name: {{ include "boundary.controller.serviceName" . }}-bootstrap-hook-config
        {{- if not .Values.tls.disabled }}
        - name: tls-certs
          secret:
            secretName: {{ .Values.tls.secretName }}
        {{- end }}
        - name: tmp
          emptyDir: {}
        - name: boundary-data
          emptyDir: {}
{{- end }}

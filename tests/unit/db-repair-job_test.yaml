# Copyright IBM Corp. 2026
# SPDX-License-Identifier: MPL-2.0

suite: Database repair job
templates:
  - templates/db-repair-job.yaml
tests:
  - it: renders the repair job when a repair version is set
    set:
      database:
        migrate:
          enabled: true
        repair:
          version: "2024-01-01-01"
    asserts:
      - isKind:
          of: Job
      - equal:
          path: metadata.name
          value: boundary-controller-database-repair
      - equal:
          path: metadata.annotations["helm.sh/hook"]
          value: pre-upgrade
      - equal:
          path: metadata.annotations["helm.sh/hook-weight"]
          value: "-10"
      - equal:
          path: metadata.annotations["helm.sh/hook-delete-policy"]
          value: before-hook-creation
      - equal:
          path: spec.backoffLimit
          value: 2
      - equal:
          path: spec.ttlSecondsAfterFinished
          value: 600
      - equal:
          path: spec.template.spec.automountServiceAccountToken
          value: false
      - equal:
          path: spec.template.spec.serviceAccountName
          value: default
      - equal:
          path: spec.template.spec.securityContext.runAsUser
          value: 100
      - equal:
          path: spec.template.spec.securityContext.fsGroupChangePolicy
          value: OnRootMismatch
      - equal:
          path: spec.template.spec.containers[0].securityContext.runAsGroup
          value: 1000
      - equal:
          path: spec.template.spec.containers[0].securityContext.seccompProfile.type
          value: RuntimeDefault
      - contains:
          path: spec.template.spec.containers[0].command
          content: sh
      - contains:
          path: spec.template.spec.containers[0].command
          content: /etc/boundary/db-repair.sh
      - contains:
          path: spec.template.spec.containers[0].command
          content: "2024-01-01-01"
      - equal:
          path: spec.template.spec.volumes[0].configMap.name
          value: boundary-controller-hook-config
  - it: renders custom image repository and pull policy in repair job
    set:
      database:
        migrate:
          enabled: true
        repair:
          version: "2024-01-01-01"
      image:
        repository: registry.example.com/boundary-enterprise
        tag: 1.2.3
        pullPolicy: Always
    asserts:
      - equal:
          path: spec.template.spec.containers[0].image
          value: registry.example.com/boundary-enterprise:1.2.3
      - equal:
          path: spec.template.spec.containers[0].imagePullPolicy
          value: Always
  - it: renders imagePullSecrets in repair job when configured
    set:
      database:
        migrate:
          enabled: true
        repair:
          version: "2024-01-01-01"
      imagePullSecrets:
        - name: regcred
    asserts:
      - contains:
          path: spec.template.spec.imagePullSecrets
          content:
            name: regcred
  - it: renders extraEnv in repair job
    set:
      database:
        migrate:
          enabled: true
        repair:
          version: "2024-01-01-01"
      extraEnv:
        - name: HTTP_PROXY
          value: http://proxy.example.com:8080
    asserts:
      - contains:
          path: spec.template.spec.containers[0].env
          content:
            name: HTTP_PROXY
            value: http://proxy.example.com:8080
  - it: renders automountServiceAccountToken when enabled in repair job
    set:
      database:
        migrate:
          enabled: true
        repair:
          version: "2024-01-01-01"
      serviceAccount:
        automountServiceAccountToken: true
    asserts:
      - equal:
          path: spec.template.spec.automountServiceAccountToken
          value: true
  - it: does not render repair job when repair version is empty
    set:
      database:
        migrate:
          enabled: true
        repair:
          version: ""
    asserts:
      - hasDocuments:
          count: 0
  - it: does not render repair job when migrate is disabled
    set:
      database:
        migrate:
          enabled: false
        repair:
          version: "2024-01-01-01"
    asserts:
      - hasDocuments:
          count: 0
  - it: renders custom TLS secret in repair job when TLS is enabled
    set:
      database:
        migrate:
          enabled: true
        repair:
          version: "2024-01-01-01"
      tls:
        disabled: false
        secretName: custom-tls-secret
    asserts:
      - contains:
          path: spec.template.spec.volumes
          content:
            name: tls-certs
            secret:
              secretName: custom-tls-secret
  - it: renders AEAD env vars when config uses env based AEAD keys in repair job
    set:
      database:
        migrate:
          enabled: true
        repair:
          version: "2024-01-01-01"
      controller:
        config: |
          kms "aead" {
            purpose = "root"
            key = "env://BOUNDARY_KMS_ROOT"
          }
      secretRefs:
        keys:
          kmsRoot: kms-root-key
          kmsWorkerAuth: kms-worker-key
          kmsRecovery: kms-recovery-key
    asserts:
      - contains:
          path: spec.template.spec.containers[0].env
          content:
            name: BOUNDARY_KMS_ROOT
            valueFrom:
              secretKeyRef:
                name: boundary-controller-secrets
                key: kms-root-key
  - it: renders migration URL env var in repair job when config uses env migration URL
    set:
      database:
        migrate:
          enabled: true
        repair:
          version: "2024-01-01-01"
      controller:
        config: |
          controller {
            database {
              migration_url = "env://BOUNDARY_PG_MIGRATION_URL"
            }
          }
      secretRefs:
        keys:
          migrationUrl: migration-url
    asserts:
      - contains:
          path: spec.template.spec.containers[0].env
          content:
            name: BOUNDARY_PG_MIGRATION_URL
            valueFrom:
              secretKeyRef:
                name: boundary-controller-secrets
                key: migration-url
  - it: renders custom database resources in repair job
    set:
      database:
        migrate:
          enabled: true
        repair:
          version: "2024-01-01-01"
        resources:
          limits:
            cpu: 900m
            memory: 1Gi
          requests:
            cpu: 200m
            memory: 256Mi
    asserts:
      - equal:
          path: spec.template.spec.containers[0].resources.limits.cpu
          value: 900m
      - equal:
          path: spec.template.spec.containers[0].resources.requests.memory
          value: 256Mi
  - it: renders nameOverride and namespace in repair job metadata
    set:
      database:
        migrate:
          enabled: true
        repair:
          version: "2024-01-01-01"
      nameOverride: custom
      namespace: boundary
    asserts:
      - equal:
          path: metadata.name
          value: custom-database-repair
      - equal:
          path: metadata.namespace
          value: boundary
